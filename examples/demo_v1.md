# 接口笔记 — 演示项目

> **⚠️ 该内容由 AI 全量扫描生成，请仔细甄别。**
> **AI 可能误判接口边界、调用关系或参数含义。**
> **请打印后在手写区修正，拍照回流后生成更精准的 v2 版。**
>
> 扫描时间：2026-07-17 05:09
> 扫描文件数：4
> 接口数量：8
> AI 置信度：中等（待人工校验）

---

> 生成时间：2026-07-17 05:09
> 接口总数：8
> 版本：v1
> 生成方式：AI全量扫描

## execute_sql 🟡
- **功能**：执行任意SQL，返回影响行数
- **参数**：
  - `sql` (str)
- **返回**：int
- **位置**：`db/query.py`
- **调用了**：（无）
- **风险**：🟡 中（涉及IO操作但未设置超时）

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## generate_report 🟡
- **功能**：API: 生成用户报告并发送邮件
- **参数**：
  - `user_id` (int)
  - `report_type` (str)
- **返回**：str
- **位置**：`api/endpoints.py`
- **调用了**：`send_email`, `query_database`
- **被调用**：`main`
- **风险**：🟡 中（涉及IO操作但未设置超时）

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## health_check 🟢
- **功能**：健康检查端点
- **参数**：（无）
- **返回**：dict
- **位置**：`api/endpoints.py`
- **调用了**：（无）
- **被调用**：`main`
- **风险**：🟢 低

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## main 🟢
- **功能**：程序主入口
- **参数**：（无）
- **返回**：（未确定）
- **位置**：`main.py`
- **调用了**：`generate_report`, `push_notification`, `health_check`
- **风险**：🟢 低

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## push_notification 🟡
- **功能**：API: 推送实时通知
- **参数**：
  - `user_id` (int)
  - `message` (str)
- **返回**：bool
- **位置**：`api/endpoints.py`
- **调用了**：`send_email`, `query_database`
- **被调用**：`main`
- **风险**：🟡 中（涉及IO操作但未设置超时）

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## query_database 🟡
- **功能**：查询数据库表，返回字典列表
- **参数**：
  - `table` (str)
  - `condition` (dict)
- **返回**：list
- **位置**：`db/query.py`
- **调用了**：（无）
- **被调用**：`generate_report`, `push_notification`
- **风险**：🟡 中（涉及IO操作但未设置超时）

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## send_bulk 🟡
- **功能**：批量发送邮件
- **参数**：
  - `emails` (list)
  - `template` (str)
- **返回**：dict
- **位置**：`utils/email.py`
- **调用了**：`send_email`
- **风险**：🟡 中（涉及IO操作但未设置超时）

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## send_email 🟡
- **功能**：发送邮件通知，支持HTML和纯文本
- **参数**：
  - `to` (str)
  - `subject` (str)
  - `body` (str)
  - `html` (bool)
- **返回**：bool
- **位置**：`utils/email.py`
- **调用了**：（无）
- **被调用**：`send_bulk`, `generate_report`, `push_notification`
- **风险**：🟡 中（涉及IO操作但未设置超时）

> 📝 手写区（打印后在此写你的理解/踩坑经验）：
>
> _______________________________________________________
>
> _______________________________________________________
>
> _______________________________________________________

---

## 📊 接口关系图

```mermaid
flowchart TD
    main["main<br/>程序主入口"]
    send_email["send_email<br/>发送邮件通知，支持HTM..."]
    send_bulk["send_bulk<br/>批量发送邮件"]
    generate_report["generate_report<br/>API: 生成用户报告并..."]
    push_notification["push_notification<br/>API: 推送实时通知"]
    health_check["health_check<br/>健康检查端点"]
    query_database["query_database<br/>查询数据库表，返回字典列..."]
    execute_sql["execute_sql<br/>执行任意SQL，返回影响..."]

    main -->|调用| generate_report
    main -->|调用| push_notification
    main -->|调用| health_check
    send_bulk -->|调用| send_email
    generate_report -->|调用| send_email
    generate_report -->|调用| query_database
    push_notification -->|调用| send_email
    push_notification -->|调用| query_database

    style main fill:#238636,stroke:#fff,color:#fff
    style send_email fill:#d29922,stroke:#fff,color:#fff
    style send_bulk fill:#d29922,stroke:#fff,color:#fff
    style generate_report fill:#d29922,stroke:#fff,color:#fff
    style push_notification fill:#d29922,stroke:#fff,color:#fff
    style health_check fill:#238636,stroke:#fff,color:#fff
    style query_database fill:#d29922,stroke:#fff,color:#fff
    style execute_sql fill:#d29922,stroke:#fff,color:#fff
```

> 🔴 红色 = 高风险 | 🟡 黄色 = 中风险 | 🟢 绿色 = 稳定
